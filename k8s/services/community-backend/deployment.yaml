apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-backend
  namespace: alphacar
  labels:
    app: community-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: community-backend
  template:
    metadata:
      labels:
        app: community-backend
        tier: backend
    spec:
      containers:
      - name: community-backend
        image: 192.168.56.200:30002/alphacar/community:latest
        ports:
        - containerPort: 3005
          name: http
        env:
        - name: PORT
          value: "3005"
        - name: SERVICE_NAME
          value: community-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: community-backend
  namespace: alphacar
  labels:
    app: community-backend
spec:
  selector:
    app: community-backend
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
  type: ClusterIP
