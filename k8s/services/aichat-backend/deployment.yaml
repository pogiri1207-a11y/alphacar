apiVersion: apps/v1
kind: Deployment
metadata:
  name: aichat-backend
  namespace: alphacar
  labels:
    app: aichat-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aichat-backend
  template:
    metadata:
      labels:
        app: aichat-backend
        tier: backend
    spec:
      containers:
      - name: aichat-backend
        image: 192.168.56.200:30002/alphacar/aichat:latest
        ports:
        - containerPort: 4000
          name: http
        env:
        - name: PORT
          value: "4000"
        - name: SERVICE_NAME
          value: aichat-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: aichat-backend
  namespace: alphacar
  labels:
    app: aichat-backend
spec:
  selector:
    app: aichat-backend
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  type: ClusterIP
