apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-backend
  namespace: alphacar
  labels:
    app: search-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-backend
  template:
    metadata:
      labels:
        app: search-backend
        tier: backend
    spec:
      containers:
      - name: search-backend
        image: 192.168.56.200:30002/alphacar/search:latest
        ports:
        - containerPort: 3007
          name: http
        env:
        - name: PORT
          value: "3007"
        - name: SERVICE_NAME
          value: search-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: search-backend
  namespace: alphacar
  labels:
    app: search-backend
spec:
  selector:
    app: search-backend
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
    name: http
  type: ClusterIP
