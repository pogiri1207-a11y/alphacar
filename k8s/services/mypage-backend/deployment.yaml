apiVersion: apps/v1
kind: Deployment
metadata:
  name: mypage-backend
  namespace: alphacar
  labels:
    app: mypage-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mypage-backend
  template:
    metadata:
      labels:
        app: mypage-backend
        tier: backend
    spec:
      containers:
      - name: mypage-backend
        image: 192.168.56.200:30002/alphacar/mypage:latest
        ports:
        - containerPort: 3006
          name: http
        env:
        - name: PORT
          value: "3006"
        - name: SERVICE_NAME
          value: mypage-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: mypage-backend
  namespace: alphacar
  labels:
    app: mypage-backend
spec:
  selector:
    app: mypage-backend
  ports:
  - port: 3006
    targetPort: 3006
    protocol: TCP
    name: http
  type: ClusterIP
