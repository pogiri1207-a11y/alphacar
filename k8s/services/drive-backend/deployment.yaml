apiVersion: apps/v1
kind: Deployment
metadata:
  name: drive-backend
  namespace: alphacar
  labels:
    app: drive-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: drive-backend
  template:
    metadata:
      labels:
        app: drive-backend
        tier: backend
    spec:
      containers:
      - name: drive-backend
        image: 192.168.56.200:30002/alphacar/drive:latest
        ports:
        - containerPort: 3008
          name: http
        env:
        - name: PORT
          value: "3008"
        - name: SERVICE_NAME
          value: drive-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3008
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: drive-backend
  namespace: alphacar
  labels:
    app: drive-backend
spec:
  selector:
    app: drive-backend
  ports:
  - port: 3008
    targetPort: 3008
    protocol: TCP
    name: http
  type: ClusterIP
