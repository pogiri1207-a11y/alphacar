apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-backend
  namespace: alphacar
  labels:
    app: main-backend
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: main-backend
  template:
    metadata:
      labels:
        app: main-backend
        tier: backend
    spec:
      containers:
      - name: main-backend
        image: 192.168.56.200:30002/alphacar/main:latest
        ports:
        - containerPort: 3002
          name: http
        env:
        - name: PORT
          value: "3002"
        - name: SERVICE_NAME
          value: main-backend
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: main-backend
  namespace: alphacar
  labels:
    app: main-backend
spec:
  selector:
    app: main-backend
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  type: ClusterIP
